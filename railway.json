{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash build.sh && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 600",
    "restartPolicyType": "ON_FAILURE"
  }
}
